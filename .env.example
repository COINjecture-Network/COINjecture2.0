# COINjecture Node — Environment Variables
# Copy this file to .env and fill in values before running docker-compose.
# Never commit .env to version control.

# ── Logging ──────────────────────────────────────────────────────────────────
# Rust log level: error | warn | info | debug | trace
RUST_LOG=info

# Set to 1 to enable full backtraces on panics (dev/staging only)
RUST_BACKTRACE=0

# ── Node Identity ─────────────────────────────────────────────────────────────
# 64-character hex address used as the mining reward recipient.
# Generate with: openssl rand -hex 32
MINER_ADDRESS=

# ── HuggingFace Integration ───────────────────────────────────────────────────
# API token for uploading problem datasets to HuggingFace Hub.
# Set here or pass --hf-token / --hf-dataset-name on the CLI.
# Get your token at: https://huggingface.co/settings/tokens
#
# Docker Compose: mining nodes load this file via env_file. Put HF_* here — do not rely on
# exporting HF_TOKEN in your shell only; the compose file must not override them with empty values.
HUGGINGFACE_TOKEN=
HF_TOKEN=
# Full Hub id: namespace/repo (verified solution sets: COINjecture/NP-Solutions)
HF_DATASET_NAME=COINjecture/NP-Solutions

# ── Network ───────────────────────────────────────────────────────────────────
# Comma-separated CPP peer addresses (host:port) to dial on startup.
# Without this, the node runs standalone (0 peers). Port is CPP P2P (default 707).
# COINJECT_BOOTNODES is preferred; BOOTNODES is an alias for the same list.
# Example: COINJECT_BOOTNODES=203.0.113.10:707,203.0.113.11:707
#
# Mesh: list **other** mesh members only — do **not** include this host's own public IP:707.
# Self-dials waste per-IP connection budget and add noise; each server should have a slightly
# different list (symmetric ring or full mesh minus self).
# Put your **canonical seed host first** (e.g. largest VPS) so bootnodes dial it before other peers:
#   on followers: COINJECT_BOOTNODES=193.203.164.13:707,198.199.81.81:707
#   on the canonical host: omit self — COINJECT_BOOTNODES=76.13.101.67:707,198.199.81.81:707
#
# Follower VPSs (sync only, no local mining): use the overlay next to docker-compose.yml:
#   docker compose -f docker-compose.yml -f docker-compose.follower-no-mine.yml up -d --no-build
COINJECT_BOOTNODES=
BOOTNODES=

# ── Container image (multi-host / production) ────────────────────────────────
# Optional: pin the node binary so every machine runs the same build (avoids divergent :latest).
# After `docker build` + `docker push`, set digest from registry UI or `docker inspect --format='{{index .RepoDigests 0}}' <image>`
# Example (uncomment and set the same value on every host):
# COINJECT_NODE_IMAGE=coinject-node@sha256:0fdadab3932ebd2ddc2583a95e90c5e6df52fcd8779d521f882342241f393031
#
# GitHub Container Registry (CI pushes on main / releases — see .github/workflows/ci.yml):
#   Package UI: https://github.com/COINjecture-Network/COINjecture2.0/pkgs/container/coinjecture2.0
#   Image:      ghcr.io/coinjecture-network/coinjecture2.0
#   Tags:       latest (main), main, sha-<gitsha>, semver tags on release.
# On each host: `docker login ghcr.io` (PAT with read:packages if private), then in .env set e.g.
#   COINJECT_NODE_IMAGE=ghcr.io/coinjecture-network/coinjecture2.0:latest
# Or pin digest (same on every host; update when you intentionally roll forward):
#   COINJECT_NODE_IMAGE=ghcr.io/coinjecture-network/coinjecture2.0@sha256:575e023fd9508d5bbec27cb0ece08a515153cbde760e0d12257549e87ddac3ba
# Deploy without compiling the node image:
#   docker compose pull bootnode node1 node2 node3   # uses COINJECT_NODE_IMAGE from .env
#   docker compose up -d --no-build bootnode node1 node2 node3 api-server
# (api-server still uses Dockerfile.api / coinject-api:latest unless you build or supply a prebuilt API image.)

# ── Storage ───────────────────────────────────────────────────────────────────
# Absolute path on the host to persist chain data outside of a Docker volume.
# Leave empty to use named Docker volumes (recommended for production).
DATA_DIR=

# ── Difficulty ────────────────────────────────────────────────────────────────
# Initial mining difficulty (number of leading zero bits required).
# Testnet default: 5 (v4 genesis)  |  v3 used 4
DIFFICULTY=5

# ── JSON-RPC (node) ───────────────────────────────────────────────────────────
# Max HTTP body for RPC (bytes). Default in code is 1 MiB — too small for chain_submitBlock via API proxy.
# 67108864 = 64 MiB (aligned with api-server DefaultBodyLimit).
RPC_MAX_BODY_BYTES=67108864
# Per-request timeout for RPC HTTP layer (seconds). Large block submit / validation may need 300+.
RPC_TIMEOUT_SECS=300

# ── Faucet (testnet only) ─────────────────────────────────────────────────────
# Amount of base units to dispense per faucet request.
FAUCET_AMOUNT=10000
